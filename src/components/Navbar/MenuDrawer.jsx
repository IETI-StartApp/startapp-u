import React from 'react';
import { Drawer } from '@material-ui/core/';

export default function MenuDrawer( ){

    return (
        <Drawer 
            anchor="left"
            variant="temporary"
        >
        </Drawer>
    );
}